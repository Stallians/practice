limit
order by asc/desc
	having

foreign key

JOIN
	left, right , none

INDEX
	- makes searching faster
	- makes another columns to store the indexes corresponding to values
	? why don't we make indexes for each columns
		- space requirements
		- slows down updation
